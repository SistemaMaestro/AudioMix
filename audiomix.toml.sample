# AudioMix configuration — copy to audiomix.toml and adjust.
# Env overrides: AUDIOMIX_SERVER__PORT=47900 (note the double underscore).

[server]
host = "0.0.0.0"
port = 47900
admin_only_localhost = true
allowed_cors_origins = [
  "https://ipb.app.br",
  "https://www.ipb.app.br",
  "http://localhost:3000",
  "http://localhost:5173",
]

[mixer]
# Empty = auto-discover on UDP 47809. Set to pin a specific IP.
host = ""
port = 53000
discovery_timeout_seconds = 8.0
# Mesa ausente (desligada): varredura branda, 15s dobrando até 2min.
idle_retry_min_seconds = 15.0
idle_retry_max_seconds = 120.0
# Conexão que existia e caiu: religa rápido, 1s dobrando até 30s.
reconnect_retry_min_seconds = 1.0
reconnect_retry_max_seconds = 30.0
# Conexão precisa durar isso para contar como boa e zerar a escada rápida.
stable_connection_seconds = 30.0
retry_jitter_pct = 0.15

[maestro]
base_url = "https://api.ipb.app.br/api"
token_cache_ttl_seconds = 300
verify_token_path = "/Auth/verificar-token"
request_timeout_seconds = 10.0

[session]
heartbeat_seconds = 5
ttl_seconds = 15

[storage]
db_path = "%LOCALAPPDATA%/AudioMix/audiomix.db"
log_path = "%LOCALAPPDATA%/AudioMix/logs/audiomix.log"

[mdns]
enabled = true
service_type = "_audiomix._tcp.local."
instance_name = "AudioMix"
hostname = "audiomix.local."
